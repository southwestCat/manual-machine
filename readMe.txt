本文件夹包含STM32F429的控制程序和Arduino的控制程序；

其中，Arduino的控制程序只是使用他来输出与输入相同的电平信号，原因是STM32 的开发板GPIO输出电压为3.3V，Arduino为5V，步进电机控制器的输入电压是5V，因此Arduino只是用来解决电压问题；

对于STM32F429的程序，可以相同的移植到STM32F109的开发板上。（使用原子编写的库，移植很方便）